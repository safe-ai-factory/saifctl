# Phase 02 — Emit output

Take the records from phase 01 and produce a JSON-serializable output that
groups them by some key. Out of scope here — this is a doc example, not a
real spec. The point is to show a feature with two phases where the second
overrides the inherited critic list.
