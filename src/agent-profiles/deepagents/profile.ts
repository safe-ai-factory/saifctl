import type { AgentProfile } from '../types.js';

/** Agent profile for the Deep Agents CLI. */
export const deepagentsProfile: AgentProfile = {
  id: 'deepagents',
  displayName: 'Deep Agents CLI',
  stdoutStrategy: null,
};
