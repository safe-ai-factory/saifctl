import type { AgentProfile } from '../types.js';

/** Agent profile for the mini-SWE-agent CLI. */
export const miniSweAgentProfile: AgentProfile = {
  id: 'mini-swe-agent',
  displayName: 'mini-SWE-agent',
  stdoutStrategy: null,
};
