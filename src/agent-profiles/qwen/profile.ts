import type { AgentProfile } from '../types.js';

/** Agent profile for the Qwen Coder CLI. */
export const qwenProfile: AgentProfile = {
  id: 'qwen',
  displayName: 'Qwen Coder',
  stdoutStrategy: null,
};
