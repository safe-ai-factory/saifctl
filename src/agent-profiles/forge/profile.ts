import type { AgentProfile } from '../types.js';

/** Agent profile for the Forge Code CLI. */
export const forgeProfile: AgentProfile = {
  id: 'forge',
  displayName: 'Forge Code',
  stdoutStrategy: null,
};
