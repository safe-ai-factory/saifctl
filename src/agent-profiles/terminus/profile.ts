import type { AgentProfile } from '../types.js';

/** Agent profile for the Terminus 2 CLI. */
export const terminusProfile: AgentProfile = {
  id: 'terminus',
  displayName: 'Terminus 2',
  stdoutStrategy: null,
};
