import type { AgentProfile } from '../types.js';

/** Agent profile for the Kilo Code CLI. */
export const kilocodeProfile: AgentProfile = {
  id: 'kilocode',
  displayName: 'Kilo Code CLI',
  stdoutStrategy: null,
};
