import type { AgentProfile } from '../types.js';

/** Agent profile for the Aider CLI. */
export const aiderProfile: AgentProfile = {
  id: 'aider',
  displayName: 'Aider',
  stdoutStrategy: null,
};
