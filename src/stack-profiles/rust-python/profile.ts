import type { StackProfile } from '../types.js';

export const rustPythonProfile: StackProfile = {
  id: 'rust-python',
  displayName: 'Rust + Python',
  coderImageTag: 'factory-coder-rust-python:latest',
  stageImageTag: 'factory-stage-rust-python:latest',
};
