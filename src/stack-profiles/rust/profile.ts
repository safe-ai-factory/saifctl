import type { StackProfile } from '../types.js';

export const rustProfile: StackProfile = {
  id: 'rust',
  displayName: 'Rust',
  coderImageTag: 'factory-coder-rust:latest',
  stageImageTag: 'factory-stage-rust:latest',
};
