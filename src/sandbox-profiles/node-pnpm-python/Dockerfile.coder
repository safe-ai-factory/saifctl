# Coder image — node-pnpm-python sandbox profile.
#
# Based on official Node.js image.
# Adds pnpm and a Python toolchain so Python-based coder agents (OpenHands, Aider via pipx,
# etc.) can be installed at runtime by agent-install.sh.
#
# Build:
#   docker build -f src/sandbox-profiles/node-pnpm-python/Dockerfile.coder \
#                -t saifctl-coder-node-pnpm-python:latest .
#
# Or via CLI:
#   pnpm docker build coder --profile node-pnpm-python
ARG NODE_VERSION=25
FROM node:${NODE_VERSION}-bookworm-slim

ENV DEBIAN_FRONTEND=noninteractive

# Add common utilities and Python toolchain.
RUN apt-get update && apt-get install -y --no-install-recommends \
  ca-certificates \
  curl \
  git \
  python3 \
  python3-venv \
  python3-pip \
  pipx \
  && rm -rf /var/lib/apt/lists/*

# Install pnpm globally.
RUN npm install -g pnpm

# Symlink pipx-managed uv into /usr/local/bin so it stays on PATH in minimal shells.
RUN pipx install uv --python python3 \
  && ln -sf /root/.local/bin/uv /usr/local/bin/uv

# Ensure pipx-managed binaries are on PATH.
ENV PATH="/root/.local/bin:$PATH"
