import type { SandboxProfile } from '../types.js';

/** Sandbox profile for Node.js (npm) + Python projects. */
export const nodeNpmPythonProfile: SandboxProfile = {
  id: 'node-npm-python',
  displayName: 'Node.js + npm + Python',
  coderImageTag: 'saifctl-coder-node-npm-python:latest',
};
