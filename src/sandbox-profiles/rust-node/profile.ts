import type { SandboxProfile } from '../types.js';

/** Sandbox profile for Rust + Node.js projects. */
export const rustNodeProfile: SandboxProfile = {
  id: 'rust-node',
  displayName: 'Rust + Node.js',
  coderImageTag: 'saifctl-coder-rust-node:latest',
};
