import type { SandboxProfile } from '../types.js';

/** Sandbox profile for Go + Node.js projects. */
export const goNodeProfile: SandboxProfile = {
  id: 'go-node',
  displayName: 'Go + Node.js',
  coderImageTag: 'saifctl-coder-go-node:latest',
};
