# Staging image — python-poetry-node sandbox profile.
#
# Provides Python 3, Poetry, and Node.js 22 LTS. Python is the primary runtime;
# Node.js is available on the side. Project dependencies are NOT installed
# here; startup.sh runs inside the container at runtime (after the workspace
# is mounted).
#
# Build:
#   docker build -f src/sandbox-profiles/python-poetry-node/Dockerfile.stage \
#                -t factory-stage-python-poetry-node:latest .
#
# Or via CLI:
#   pnpm docker build stage --profile python-poetry-node
FROM python:3.13-slim

ENV DEBIAN_FRONTEND=noninteractive

WORKDIR /workspace

# Install Node.js 22 LTS to match Dockerfile.coder (equivalent environments).
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    pipx \
    && curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
    && apt-get install -y --no-install-recommends nodejs \
    && rm -rf /var/lib/apt/lists/*

RUN pipx install poetry
ENV PATH="/root/.local/bin:$PATH"
