# saifctl version

Print the **installed** saifctl package version.

## Usage

```bash
saifctl version
```

This subcommand takes no arguments.

The same version is shown when you run:

```bash
saifctl --version
```

## Examples

```bash
saifctl version
```

```text
0.0.1
```
