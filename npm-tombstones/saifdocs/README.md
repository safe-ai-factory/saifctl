# saifdocs (tombstone)

The **official** Saifdocs package is published under the org scope:

```bash
npm install @safe-ai-factory/saifdocs
```

The `saifdocs` CLI binary is provided by that package. This unscoped name exists only to prevent namespace squatting.
